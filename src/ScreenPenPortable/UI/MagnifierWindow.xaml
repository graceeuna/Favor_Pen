<Window x:Class="ScreenPenPortable.UI.MagnifierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Magnifier"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Focusable="False"
        IsHitTestVisible="False"
        SnapsToDevicePixels="True">
    <!-- 둥근 테두리 프레임 + 내부 확대 이미지.
         IsHitTestVisible=False / Focusable=False: 돋보기는 순수 표시용이라 입력을 가로채지 않는다. -->
    <Border x:Name="FrameBorder"
            BorderBrush="#FF00C8FF"
            BorderThickness="3"
            CornerRadius="6"
            Background="#FF101010"
            Padding="2">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>
        <Image x:Name="ZoomImage"
               Stretch="Fill"
               RenderOptions.BitmapScalingMode="NearestNeighbor"/>
    </Border>
</Window>
